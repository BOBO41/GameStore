@model GameStore.Models.Game

<form action="" method="POST">
    <div class="form-group">
        <label for="GameName">Game</label>
        <input asp-for="Name" class="form-control">
    </div>
    <div class="form-group">
        <label for="DescriptionOfTheGame">Description</label>
        <textarea asp-for="Description" class="form-control"/>
    </div>
    <div class="form-group">
        <label for="GamePrice">Price</label>
        <input asp-for="Price" class="form-control">
    </div>
    <div class="form-group">
        <label for="ID">Consoles</label>
        <select asp-for="Consoles" 
                    asp-items="@(new SelectList(Model.Consoles,"ID","Name"))"/>
        <option>Please select one</option>
    </select>
    </div>
    <div class="form-group">
        <label for="Company">Game</label>
        <select asp-for="Consoles" 
                    asp-items="@(new SelectList(Model.Company,"ID","Name"))"/>
        <option>Please select one</option>
    </div>
</form>